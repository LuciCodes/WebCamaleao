import { BusinessAreaNamePipe } from './business-area-name.pipe';

describe('BusinessAreaNamePipe', () => {
  it('create an instance', () => {
    const pipe = new BusinessAreaNamePipe();
    expect(pipe).toBeTruthy();
  });
});
