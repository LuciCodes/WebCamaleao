import { CourseLevelNamePipe } from './course-level-name.pipe';

describe('CourseLevelNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseLevelNamePipe();
    expect(pipe).toBeTruthy();
  });
});
