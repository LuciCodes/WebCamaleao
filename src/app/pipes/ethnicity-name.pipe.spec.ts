import { EthnicityNamePipe } from './ethnicity-name.pipe';

describe('EthnicityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new EthnicityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
